[project]
name = "modelling-testing"
version = "0.1.0"
description = "Testing pod: OpenLLM serving and model evaluation on GPU"
requires-python = ">=3.12"
dependencies = [
    "vllm",
    "transformers",
    "huggingface-hub",
    "hf-transfer",
    "safetensors",
    "sentencepiece",
    "datasets",
    "requests",
]
